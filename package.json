{
  "name": "souomeneses",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "build": "astro build",
    "preview": "astro preview --host 127.0.0.1",
    "test": "node check.mjs",
    "deploy": "npm run build && npm test && npx --yes wrangler@4.135.0 deploy"
  },
  "dependencies": {
    "astro": "^7.3.3"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
